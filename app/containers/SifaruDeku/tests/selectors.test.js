// import { fromJS } from 'immutable';
// import { makeSelectSifaruDekuDomain } from '../selectors';

// const selector = makeSelectSifaruDekuDomain();

describe('makeSelectSifaruDekuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
